export type ScenarioRole = "SUPER_ADMIN" | "SALES" | "HEAD_SA" | "SA";
export interface WorkflowStage { id: string; scenario_id: string; name: string; description?: string | null; step_order: number; default_role: ScenarioRole; default_duration_working_days: number; is_required: boolean; is_active: boolean; created_at: string; updated_at: string; }
export interface Scenario { id: string; name: string; description?: string | null; is_active: boolean; created_at: string; updated_at: string; total_steps: number; }
export interface WorkflowResponse { scenario: Pick<Scenario, "id" | "name">; stages: WorkflowStage[]; }