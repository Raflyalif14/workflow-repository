{
  "name": "workflow-repository-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Workflow Repository Management System Monorepo",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "keywords": [
    "nextjs",
    "express",
    "typescript",
    "supabase",
    "workflow",
    "repository"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4"
  }
}
